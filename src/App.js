import './App.css';
import Header from './Header';
import Quote from './Quote';
import Button from './Button';

function App() {
  return (
    <div className="container">
          {/*
          header component
          quote component
          button component
          getQoute component
          */}
          <Header/>
          <Quote/>
          <Button/>
    </div>
  );
}

export default App;
